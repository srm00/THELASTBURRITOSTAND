
public class Burrito {
	
	
	public double calcPrice()
	{
		double price = 0; 
		MaxHeap1 priceHeap = new MaxHeap1(8);
		
		for(int i = 0; i<choices.length(); i++)
		{
		priceHeap.insert(this.getPrice(choices[i])); //cookedBurrito is the completed array of Strings of what the customer wanted (w/out blanks)
		}
		
		price = priceHeap.deleteMax(); 
		price = price + priceHeap.deleteMax(); 
	    price = price + priceHeap.deleteMax();
		
		
		return price; 
	}

	
}
